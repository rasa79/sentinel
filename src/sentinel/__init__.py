"""Sentinel — Agentic Incident Triage Assistant (SRE Copilot).

The package root is intentionally small; the CLI entry point lives at
``sentinel.cli.main:app`` (see ``[project.scripts]`` in ``pyproject.toml``).
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
